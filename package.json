{
  "name": "gsip",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source citizen-science soil mapping",
  "license": "MIT",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r build",
    "build:pages": "pnpm build && node scripts/build-pages.mjs",
    "check:map-budget": "node scripts/check-map-budget.mjs",
    "check:site-budget": "node scripts/check-site-budget.mjs",
    "db:reset": "supabase db reset",
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:test": "supabase test db",
    "format": "prettier --write .",
    "lint": "eslint . && node scripts/check-language.mjs && prettier --check .",
    "test": "pnpm -r test",
    "test:e2e": "playwright test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.61.1",
    "eslint": "10.7.0",
    "prettier": "3.9.6",
    "supabase": "2.109.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.65.0",
    "vitest": "4.1.10"
  }
}
